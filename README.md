# policast
Author governance once in Cedar, compile it to portable CEL expressions, and push enforcement into the query engine's physical plan. No policy logic in your SQL. No per-engine rewrites. One manifest, many engines.
